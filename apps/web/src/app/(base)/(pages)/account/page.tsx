type Props = {
  params: { handle: string }
}
const AccountPage = (props: Props) => {
  const { params } = props

  return (
    <div>
      <>アカウントページ</>
    </div>
  )
}

export default AccountPage
